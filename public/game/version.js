// Build version shown on the title screen. This "dev" value is the placeholder used
// when running the source directly (local http.server); tools/package.sh overwrites
// it with a real date+SHA stamp at package time. See the versioning spec.
export const VERSION = "dev";
